import { Policy } from '../../shared/types';

const CREATED_AT = '2026-04-09T09:00:00.000Z';

const baseGrouping = {
  groupBy: [],
  groupWait: '30s',
  groupInterval: '5m',
  repeatInterval: '4h',
};

export const mockPolicies: Policy[] = [
  {
    id: 'default-policy',
    name: 'Default (Root)',
    description: 'Default fallback policy',
    enabled: true,
    matchers: [],
    actions: [
      {
        channel: 'email',
        receivers: ['sre-team@example.com'],
      },
    ],
    grouping: {
      groupBy: ['alertname'],
      groupWait: '30s',
      groupInterval: '5m',
      repeatInterval: '4h',
    },
    isDefault: true,
    createdAt: CREATED_AT,
    children: [],
    parentId: null,
    messageTemplate: 'default',
    repeatIntervalType: 'default',
    notificationMode: 'notify',
    channelReceivers: {
      email: ['sre-team@example.com'],
      dingtalk: [],
      wechat: [],
      feishu: [],
      webhook: [],
      insite: [],
      sms: [],
    },
  },
  {
    id: 'sev-critical',
    name: 'Critical Alerts',
    description: 'Severity = Critical',
    enabled: true,
    matchers: [{ label: 'severity', operator: '=', values: ['critical'] }],
    actions: [],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'default-policy',
    children: [],
    continue: true,
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-critical-prod',
    name: 'Critical Alerts - PROD',
    description: 'Cluster = prod',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['prod'] }],
    actions: [{ channel: 'email', receivers: ['prod-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-critical',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-critical-test',
    name: 'Critical Alerts - TEST',
    description: 'Cluster = test',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['test'] }],
    actions: [{ channel: 'email', receivers: ['test-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-critical',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-critical-dev',
    name: 'Critical Alerts - DEV',
    description: 'Cluster = dev',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['dev'] }],
    actions: [{ channel: 'email', receivers: ['dev-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-critical',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-major',
    name: 'Major Alerts',
    description: 'Severity = Major',
    enabled: true,
    matchers: [{ label: 'severity', operator: '=', values: ['major'] }],
    actions: [],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'default-policy',
    children: [],
    continue: true,
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-major-prod',
    name: 'Major Alerts - PROD',
    description: 'Cluster = prod',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['prod'] }],
    actions: [{ channel: 'email', receivers: ['prod-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-major',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-major-test',
    name: 'Major Alerts - TEST',
    description: 'Cluster = test',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['test'] }],
    actions: [{ channel: 'email', receivers: ['test-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-major',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-major-dev',
    name: 'Major Alerts - DEV',
    description: 'Cluster = dev',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['dev'] }],
    actions: [{ channel: 'email', receivers: ['dev-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-major',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-minor',
    name: 'Minor Alerts',
    description: 'Severity = Minor',
    enabled: true,
    matchers: [{ label: 'severity', operator: '=', values: ['minor'] }],
    actions: [],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'default-policy',
    children: [],
    continue: true,
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-minor-prod',
    name: 'Minor Alerts - PROD',
    description: 'Cluster = prod',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['prod'] }],
    actions: [{ channel: 'email', receivers: ['prod-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-minor',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-minor-test',
    name: 'Minor Alerts - TEST',
    description: 'Cluster = test',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['test'] }],
    actions: [{ channel: 'email', receivers: ['test-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-minor',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'sev-minor-dev',
    name: 'Minor Alerts - DEV',
    description: 'Cluster = dev',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['dev'] }],
    actions: [{ channel: 'email', receivers: ['dev-team@example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'sev-minor',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'team-routing',
    name: 'Team Routing',
    description: 'Route to specific teams based on label',
    enabled: true,
    matchers: [{ label: 'alertname', operator: '=', values: ['TestAlert'] }],
    actions: [],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'default-policy',
    children: [],
    continue: true,
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
  {
    id: 'team-routing-prod',
    name: 'Team Routing - PROD',
    description: 'Team routing for prod',
    enabled: true,
    matchers: [{ label: 'cluster_name', operator: '=', values: ['prod'] }],
    actions: [{ channel: 'webhook', receivers: ['http://team.example.com'] }],
    grouping: baseGrouping,
    isDefault: false,
    createdAt: CREATED_AT,
    parentId: 'team-routing',
    children: [],
    messageTemplate: 'default',
    repeatIntervalType: 'default',
  },
];

export const cloneMockPolicies = (): Policy[] => JSON.parse(JSON.stringify(mockPolicies));
