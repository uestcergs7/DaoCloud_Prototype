{"name": "daocloud-prototype-monorepo","version": "1.0.0","scripts": {"build": "cd gatewayapi/ui-prototype && npm install && npm run build && cd ../../portal && npm install && npm run build && cd .. && rm -rf dist && mkdir -p dist/prototypes/gatewayapi && cp -r portal/dist/* dist/ && cp -r gatewayapi/ui-prototype/dist/* dist/prototypes/gatewayapi/"}}
